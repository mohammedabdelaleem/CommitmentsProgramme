@{
    string message = TempData["success"]?.ToString() ?? "";
}

@if (!string.IsNullOrWhiteSpace(message))
{
    <script>
        const text = "@message";

        if (text.includes("Created") || text.includes("اضافة")) {
            toastr.success(text);
        }
        else if (text.includes("Updated") || text.includes("تعديل")) {
            toastr.info(text);
        }
        else if (text.includes("Deleted") || text.includes("حذف")) {
            toastr.error(text); 
        }
        else {
            toastr.warning(text);
        }
    </script>
}

@if (TempData["error"] != null)
{
    <script>
        toastr.error("@TempData["error"]");
    </script>
}
