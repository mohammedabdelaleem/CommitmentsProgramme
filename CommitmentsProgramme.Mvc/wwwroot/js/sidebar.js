//let sidebar = document.getElementsByClassName("sidebar")[0];

//sidebar.addEventListener('click', (eo) => {
//  //eo.preventDefault();

//  //  Finds nearest parent <a> no matter where user clicks even if : <a><i><span>
//  const link = eo.target.closest('.nav-link')
//  if (!link) return;

//  document.querySelectorAll('.sidebar .nav-link').forEach(item => {
//    item.classList.remove('active-link')
//  })

//  link.classList.add('active-link')

//  console.log(`${link} `)
//}); 